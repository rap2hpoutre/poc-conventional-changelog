{
  "name": "conventional",
  "version": "2.1.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "standard-version",
    "build": "echo \"Build\"",
    "merge-master-in-devel": "git fetch origin && git checkout devel && git pull origin && git merge master && git push origin",
    "merge-devel-in-master": "git fetch origin && git checkout master && git pull origin && git merge devel && git push origin"
  },
  "standard-version": {
    "scripts": {
      "prerelease": "npm run build && npm run merge-master-in-devel && npm run merge-devel-in-master"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rap2hpoutre/poc-conventional-changelog.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rap2hpoutre/poc-conventional-changelog/issues"
  },
  "homepage": "https://github.com/rap2hpoutre/poc-conventional-changelog#readme",
  "devDependencies": {
    "standard-version": "^7.0.0"
  }
}
